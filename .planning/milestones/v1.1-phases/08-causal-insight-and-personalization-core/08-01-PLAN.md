---
phase: 08-causal-insight-and-personalization-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/aira/health/domain/model/CausalFactor.kt
  - app/src/main/java/com/aira/health/domain/model/CausalInsightSnapshot.kt
  - app/src/main/java/com/aira/health/domain/engine/CausalRankingEngine.kt
  - app/src/main/java/com/aira/health/domain/usecase/ComputeCausalInsightsUseCase.kt
  - app/src/main/java/com/aira/health/data/local/model/CausalInsight.kt
  - app/src/main/java/com/aira/health/data/local/dao/CausalInsightDao.kt
  - app/src/main/java/com/aira/health/data/local/db/AiraDatabase.kt
  - app/src/main/java/com/aira/health/di/DatabaseModule.kt
  - app/src/main/java/com/aira/health/domain/usecase/ComputeDailyScoresUseCase.kt
  - app/src/test/java/com/aira/health/domain/engine/CausalRankingEngineTest.kt
  - app/src/test/java/com/aira/health/domain/usecase/ComputeCausalInsightsUseCaseTest.kt
  - app/src/test/java/com/aira/health/domain/usecase/ComputeDailyScoresUseCaseTest.kt
autonomous: true
requirements: [CAUS-01, CAUS-02]
must_haves:
  truths:
    - "Each metric insight is backed by exactly three ranked contributing factors from real recent user windows (not static template bullets)."
    - "Every ranked factor has directional effect and numeric contribution weight suitable for deterministic UI rendering."
    - "Causal ranking runs in local daily scoring flow and persists on-device only."
  artifacts:
    - path: "app/src/main/java/com/aira/health/domain/engine/CausalRankingEngine.kt"
      provides: "Deterministic top-3 factor ranking with directional impact, recency weighting, and tie-break policy."
      exports: ["CausalRankingEngine"]
    - path: "app/src/main/java/com/aira/health/domain/usecase/ComputeCausalInsightsUseCase.kt"
      provides: "Pipeline from DailyMetrics windows to persisted ranked causal snapshot."
      exports: ["ComputeCausalInsightsUseCase"]
    - path: "app/src/main/java/com/aira/health/data/local/model/CausalInsight.kt"
      provides: "Room entity for storing factors, recency window metadata, and confidence scalar."
      contains: "@Entity(tableName = \"causal_insights\")"
    - path: "app/src/main/java/com/aira/health/data/local/dao/CausalInsightDao.kt"
      provides: "Read/write DAO for latest-by-metric and date-window causal insight retrieval."
      exports: ["upsert", "observeLatestByMetric", "getByDateRange"]
  key_links:
    - from: "app/src/main/java/com/aira/health/domain/usecase/ComputeDailyScoresUseCase.kt"
      to: "app/src/main/java/com/aira/health/domain/usecase/ComputeCausalInsightsUseCase.kt"
      via: "post-score computation hook in same daily transaction boundary"
      pattern: "computeForDate.*computeCausalInsights"
    - from: "app/src/main/java/com/aira/health/domain/usecase/ComputeCausalInsightsUseCase.kt"
      to: "app/src/main/java/com/aira/health/data/local/dao/CausalInsightDao.kt"
      via: "persist top ranked factors"
      pattern: "causalInsightDao\.upsert"
    - from: "app/src/main/java/com/aira/health/domain/engine/CausalRankingEngine.kt"
      to: "app/src/main/java/com/aira/health/domain/model/CausalFactor.kt"
      via: "ranked factor contract"
      pattern: "direction|weight|windowLabel"
---

<objective>
Implement the causal feature extraction and deterministic factor ranking pipeline that converts recent local telemetry windows into persisted top-3 factor snapshots per metric.

Purpose: Satisfy CAUS-01 and CAUS-02 with real-data explainability groundwork for Phase 08.
Output: Domain ranking engine, use-case orchestration, Room persistence contract, and unit coverage for deterministic ordering.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-causal-insight-and-personalization-core/08-CONTEXT.md
@.planning/phases/08-causal-insight-and-personalization-core/08-RESEARCH.md
@.planning/phases/08-causal-insight-and-personalization-core/08-VALIDATION.md
@.planning/phases/08-causal-insight-and-personalization-core/08-UI-SPEC.md
@.planning/phases/07-on-device-ai-runtime-foundation/07-01-SUMMARY.md
@.planning/phases/07-on-device-ai-runtime-foundation/07-02-SUMMARY.md
@.planning/phases/07-on-device-ai-runtime-foundation/07-AI-SPEC.md
@app/src/main/java/com/aira/health/domain/usecase/ComputeDailyScoresUseCase.kt
@app/src/main/java/com/aira/health/data/local/model/DailyMetrics.kt
@app/src/main/java/com/aira/health/data/local/db/AiraDatabase.kt

<interfaces>
From app/src/main/java/com/aira/health/data/local/model/DailyMetrics.kt:
```kotlin
@Entity(tableName = "daily_metrics")
data class DailyMetrics(
    @PrimaryKey val date: String,
    val recoveryScore: Int,
    val sleepScore: Int,
    val strainScore: Int,
    val stressScore: Int,
    val dataConfidence: Float,
    val calculatedAt: Long
)
```

From app/src/main/java/com/aira/health/domain/usecase/ComputeDailyScoresUseCase.kt:
```kotlin
suspend fun computeForDate(
    date: String,
    hrvMorning: Float?,
    rhrMorning: Float?,
    sleepDurationMin: Int?,
    sleepEfficiency: Float?,
    ...
)
```

From app/src/main/java/com/aira/health/data/local/db/AiraDatabase.kt:
```kotlin
@Database(
  entities = [ ... ],
  version = 4,
  exportSchema = true
)
abstract class AiraDatabase : RoomDatabase()
```
</interfaces>
</context>

<source_audit>
GOAL:
- Build explainability engine converting telemetry history to trustworthy individualized reasoning -> COVERED by Task 1, Task 2, Task 3.

REQ:
- CAUS-01 -> COVERED by Task 1 and Task 2 (ranked top contributors).
- CAUS-02 -> COVERED by Task 1 and Task 2 (real recent data windows in ranking inputs and persisted output).

RESEARCH:
- Deterministic top-3 ranking with directional impact and recency weighting -> COVERED by Task 1 and Task 2.
- Tie-break handling for near-equal weights -> COVERED by Task 1.
- Persist causal output for presentation consumption -> COVERED by Task 2 and Task 3.

CONTEXT:
- D-01 top 3 weighted factors -> COVERED by Task 1 (per D-01).
- D-02 directional impact and contribution weight -> COVERED by Task 1 (per D-02).
- Discretion: near-equal tie-break rule -> COVERED by Task 1 using deterministic comparator.
- No deferred ideas present.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define causal contracts and deterministic ranking rules</name>
  <files>app/src/main/java/com/aira/health/domain/model/CausalFactor.kt, app/src/main/java/com/aira/health/domain/model/CausalInsightSnapshot.kt, app/src/main/java/com/aira/health/domain/engine/CausalRankingEngine.kt, app/src/test/java/com/aira/health/domain/engine/CausalRankingEngineTest.kt</files>
  <behavior>
    - Test 1: Ranking returns exactly three factors ordered by contribution weight (per D-01).
    - Test 2: Each factor exposes direction and weight fields with stable sign behavior (per D-02).
    - Test 3: If absolute weight delta is below 0.03, tie-break by newer window then key ascending.
  </behavior>
  <action>Create domain contracts for ranked factor output, including metric key, direction enum, contribution weight, and explicit window label. Implement CausalRankingEngine to compute candidates from recent local windows (24h, 72h, 7d), normalize contributions to [0,1], and return top 3 only (per D-01). Include deterministic tie-break for near-equal factors (absolute delta < 0.03) by recency desc then factor key asc (discretion area). Ensure outputs are local-only and contain no network serialization path to preserve privacy-first boundary.</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "*CausalRankingEngineTest"</automated>
  </verify>
  <done>Ranking engine contract is implemented, deterministic, and test-proven for top-3 and directional weighting rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement causal insight use case and Room persistence model</name>
  <files>app/src/main/java/com/aira/health/domain/usecase/ComputeCausalInsightsUseCase.kt, app/src/main/java/com/aira/health/data/local/model/CausalInsight.kt, app/src/main/java/com/aira/health/data/local/dao/CausalInsightDao.kt, app/src/main/java/com/aira/health/data/local/db/AiraDatabase.kt, app/src/main/java/com/aira/health/di/DatabaseModule.kt, app/src/test/java/com/aira/health/domain/usecase/ComputeCausalInsightsUseCaseTest.kt</files>
  <behavior>
    - Test 1: Use case persists one latest row per metric/date with top-3 factors only.
    - Test 2: Persisted factors include real source window labels (24h/72h/7d) and timestamps from local records.
    - Test 3: No static template fallback factors are stored when input evidence exists.
  </behavior>
  <action>Create ComputeCausalInsightsUseCase that consumes DailyMetrics and relevant local signals, invokes CausalRankingEngine, and writes a CausalInsight row transactionally via CausalInsightDao. Add Room entity and DAO for latest-by-metric observation and date-range retrieval. Update AiraDatabase entities/dao methods and DatabaseModule providers. Keep all data local in SQLCipher Room storage and do not add Supabase/network writes for this pipeline, preserving raw biometric off-device prohibition.</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "*ComputeCausalInsightsUseCaseTest" --tests "*CausalRankingEngineTest"</automated>
  </verify>
  <done>Causal insight snapshots persist with ranked factors and real recency-window evidence ready for UI consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire causal computation into daily scoring flow</name>
  <files>app/src/main/java/com/aira/health/domain/usecase/ComputeDailyScoresUseCase.kt, app/src/main/java/com/aira/health/domain/usecase/ComputeCausalInsightsUseCase.kt, app/src/test/java/com/aira/health/domain/usecase/ComputeDailyScoresUseCaseTest.kt</files>
  <behavior>
    - Test 1: computeForDate triggers causal insight update after score persistence for the same date.
    - Test 2: causal update failure does not leak raw exceptions to UI flow and is logged safely.
  </behavior>
  <action>Inject ComputeCausalInsightsUseCase into ComputeDailyScoresUseCase and invoke it after DailyMetrics upsert so causal ranking stays synchronized with score outputs for the same day. Keep execution inside local domain layer and avoid outbound transport of raw telemetry. Add/update unit test coverage in ComputeDailyScoresUseCaseTest to assert orchestration call order and non-network behavior assumptions.</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "*ComputeDailyScoresUseCaseTest" --tests "*ComputeCausalInsightsUseCaseTest"</automated>
  </verify>
  <done>Daily scoring pipeline now produces persisted causal top-3 outputs without introducing off-device data flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local metrics/corrections -> ranking engine | Health-derived inputs cross into causal attribution calculations. |
| ranking engine -> Room persistence | Ranked outputs and metadata are materialized to local database records. |
| domain pipeline -> future UI read path | Explainability output crosses from domain calculations into user-facing surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | CausalRankingEngine input windows | mitigate | Validate date-window boundaries and metric ranges before scoring; reject malformed/empty windows instead of fabricating factors. |
| T-08-02 | R | ComputeCausalInsightsUseCase | mitigate | Persist ranking timestamp, source-window labels, and metric key for auditability of why a factor appeared. |
| T-08-03 | I | CausalInsight persistence | mitigate | Store only aggregated causal factors/weights; do not persist raw sample payloads in causal_insights table. |
| T-08-04 | D | Daily scoring integration | mitigate | Keep ranking computation bounded to fixed windows (24h/72h/7d) and cap candidate factor count before sort. |
| T-08-05 | E | Database surface | mitigate | Expose writes only through domain use case and DAO methods; no external export/sync endpoint for raw causal inputs. |
</threat_model>

<verification>
- .\gradlew.bat :app:testDevDebugUnitTest --tests "*CausalRankingEngineTest" --tests "*ComputeCausalInsightsUseCaseTest"
- .\gradlew.bat :app:testDevDebugUnitTest --tests "*ComputeDailyScoresUseCaseTest"
- .\gradlew.bat :app:compileDevDebugKotlin
</verification>

<success_criteria>
- Causal ranking returns exactly top 3 weighted factors with directional impact for each metric insight.
- Persisted causal outputs are derived from real local recent windows and retrievable via DAO.
- Daily scoring flow triggers causal pipeline without adding any raw-biometric off-device path.
</success_criteria>

<output>
After completion, create .planning/phases/08-causal-insight-and-personalization-core/08-01-SUMMARY.md
</output>
