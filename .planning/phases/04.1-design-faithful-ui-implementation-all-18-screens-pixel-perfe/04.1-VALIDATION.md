# Phase 04.1 Validation Report

Date: 2026-04-16
Status: pass (automation) / blocked (device UAT replay unavailable in environment)

## Scope

Validation covers the 04.1 remediation pass for:

- Locked IA route compliance (Home, Insights, Train, Nutrition, Settings)
- Home 2x2 clinical score layout and canonical anomaly card integration
- Settings and supplementary runtime-state wiring (including Data Confidence route)
- Health pipeline correction from null score inputs to Room/repository-derived inputs
- No-runtime-mock-data gate and core compile/test checkpoints

## Executed Verification

1. Main-source compile

- Command: `./gradlew.bat :app:compileDevDebugKotlin`
- Result: PASS

2. Full unit suite

- Command: `./gradlew.bat --no-daemon :app:testDevDebugUnitTest`
- Result: PASS
- Note: first daemon-backed run failed due transient Windows file lock on generated `R.jar`; no test assertion failures were reported.

3. Android test source compile

- Command: `./gradlew.bat :app:compileDevDebugAndroidTestKotlin`
- Result: PASS

4. Global no-runtime-mock-data contract

- Command: `./gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.presentation.supplementary.NoRuntimeMockDataContractTest"`
- Result: PASS

5. Device-level UAT replay attempt

- Command: `adb devices`
- Result: FAIL (`CommandNotFoundException` in local shell)

- Command: `./gradlew.bat :app:connectedDevDebugAndroidTest`
- Result: FAIL (`No connected devices!`)

6. Targeted UAT-linked automation rerun

- Command: `./gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.presentation.dashboard.home.HomeViewModelTest" --tests "com.aira.health.presentation.nutrition.NutritionViewModelTest" --tests "com.aira.health.presentation.train.TrainViewModelTest" --tests "com.aira.health.presentation.navigation.DeepLinkRouterTest" --tests "com.aira.health.presentation.dashboard.details.MetricDetailViewModelTest" --tests "com.aira.health.presentation.supplementary.NoRuntimeMockDataContractTest"`
- Result: PASS

## Implemented Remediations

- Route/IA alignment:
  - Bottom nav now reflects locked tabs (Home/Insights/Train/Nutrition/Settings).
  - Deep-link router normalizes legacy aliases and preserves safe fallback behavior.
- Home contract alignment:
  - Fixed 2x2 clinical grid now renders Recovery/Sleep/Strain/Stress.
  - Home uses canonical `CausalAnomalyCard` (always-visible fallback behavior).
- Supplementary and settings hardening:
  - Added state-driven `InsightsPredictionsViewModel` and `DataCorrectionsViewModel`.
  - Added dedicated `DataConfidenceScreen` route and Settings navigation hooks.
  - Settings moved from static literals to runtime-derived profile/sync/model/confidence state.
- Health ingestion/score pipeline correction:
  - `HealthSyncWorker` now derives real daily inputs from Room (HR/HRV/Sleep/Workouts) and repository reads (SpO2/steps/calories) before score computation.
  - `ComputeDailyScoresUseCase` now persists `totalSteps`, `activeCalories`, and `spo2` into `DailyMetrics`.

## Residual Risks / Follow-ups

- Device-level UAT replay is blocked in this environment due no connected device/emulator.
- Skin temperature remains unpopulated in daily metrics in this pass; current compute path persists it as `null`.
- Non-blocking icon deprecation warnings remain (Material auto-mirrored migration).

## Conclusion

Automated gates are green for this repository state (compile + unit + androidTest compile + no-mock gate + targeted UAT-linked unit suites). Device replay was attempted but is currently blocked by environment hardware availability; final experiential closure still depends on device evidence in `04-UAT.md`.
