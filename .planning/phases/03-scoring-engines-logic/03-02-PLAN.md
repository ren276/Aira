---
phase: 03-scoring-engines-logic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/aira/health/domain/engine/StrainEngine.kt
  - app/src/main/java/com/aira/health/domain/engine/StressEngine.kt
  - app/src/main/java/com/aira/health/domain/engine/EnergyBankEngine.kt
  - app/src/test/java/com/aira/health/domain/engine/StrainEngineTest.kt
  - app/src/test/java/com/aira/health/domain/engine/StressEngineTest.kt
  - app/src/test/java/com/aira/health/domain/engine/EnergyBankEngineTest.kt
autonomous: true
requirements:
  - SCORE-03
  - SCORE-04
must_haves:
  truths:
    - User receives Strain and Stress scores that emphasize physiological extremes through non-linear scaling.
    - User receives a visible Energy Bank score while internal depletion/recharge state is preserved.
    - Low-confidence days still show scores with confidence signaling rather than suppression.
  artifacts:
    - path: app/src/main/java/com/aira/health/domain/engine/StrainEngine.kt
      provides: Zone-based non-linear Strain scoring model.
    - path: app/src/main/java/com/aira/health/domain/engine/StressEngine.kt
      provides: Hourly rolling Stress scoring model with non-linear daily projection.
    - path: app/src/main/java/com/aira/health/domain/engine/EnergyBankEngine.kt
      provides: Hybrid public-score plus internal-state Energy Bank model.
    - path: app/src/test/java/com/aira/health/domain/engine/StrainEngineTest.kt
      provides: Boundary and monotonic tests for Strain behavior.
    - path: app/src/test/java/com/aira/health/domain/engine/StressEngineTest.kt
      provides: Hourly and daily stress behavior tests.
    - path: app/src/test/java/com/aira/health/domain/engine/EnergyBankEngineTest.kt
      provides: Energy Bank depletion/recharge and confidence behavior tests.
  key_links:
    - from: app/src/main/java/com/aira/health/domain/engine/StrainEngine.kt
      to: app/src/test/java/com/aira/health/domain/engine/StrainEngineTest.kt
      via: Zone load -> exponential scale mapping.
      pattern: Zone 4/5 minutes push output toward upper bound faster than linear
    - from: app/src/main/java/com/aira/health/domain/engine/StressEngine.kt
      to: app/src/test/java/com/aira/health/domain/engine/StressEngineTest.kt
      via: Hourly stress snapshots roll into daily stress with bounded projection.
      pattern: hourly rolling output + daily aggregate in 0..100
    - from: app/src/main/java/com/aira/health/domain/engine/EnergyBankEngine.kt
      to: app/src/test/java/com/aira/health/domain/engine/EnergyBankEngineTest.kt
      via: Recovery recharge and Strain/Stress depletion influence visible and internal values.
      pattern: visible score and internal balance both updated per day
---

<objective>
Implement Strain, Stress, and Energy Bank engines as deterministic pure Kotlin components using non-linear scaling and hybrid state semantics.

Purpose: Deliver SCORE-03 and SCORE-04 with physiologically meaningful high-end sensitivity.
Output: StrainEngine, StressEngine, EnergyBankEngine, and dedicated unit tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-engines-logic/03-CONTEXT.md
@.planning/phases/03-scoring-engines-logic/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build StrainEngine with non-linear zone scaling (D-01)</name>
  <files>app/src/main/java/com/aira/health/domain/engine/StrainEngine.kt, app/src/test/java/com/aira/health/domain/engine/StrainEngineTest.kt</files>
  <behavior>
    - Test 1: Zone-based input produces bounded 0..100 strain values.
    - Test 2: Zone-5-heavy workloads produce disproportionately higher scores than linear models (per D-01).
    - Test 3: Sparse zone data still returns score + reduced confidence rather than suppression (D-03, D-04, D-11, D-12).
  </behavior>
  <action>Implement SCORE-04 by aggregating time-in-zones (1-5) into weighted load and applying exponential/non-linear projection so high-intensity days converge toward 100 faster than linear mapping (D-01). Keep confidence as parallel output and never hide score when some inputs are absent (D-03, D-04, D-11, D-12).</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.StrainEngineTest"</automated>
  </verify>
  <done>StrainEngine exists with non-linear high-end behavior and deterministic tests proving boundary/monotonic properties.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build StressEngine with hourly rolling computation (D-01)</name>
  <files>app/src/main/java/com/aira/health/domain/engine/StressEngine.kt, app/src/test/java/com/aira/health/domain/engine/StressEngineTest.kt</files>
  <behavior>
    - Test 1: Hourly stress snapshots are emitted for a day without exceeding 0..100.
    - Test 2: Daily stress aggregate reflects non-linear amplification of extreme hours (D-01).
    - Test 3: Missing hourly windows degrade confidence but still return a score (D-03, D-04, D-11, D-12).
  </behavior>
  <action>Implement SCORE-03 hourly rolling stress using HR/HRV-vs-baseline deltas and non-linear scaling for high-stress spikes per D-01. Expose both hourly outputs and daily aggregate from one deterministic API to support downstream explanations. Preserve score visibility when data is partial, with confidence downgrade only (D-03, D-04, D-11, D-12).</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.StressEngineTest"</automated>
  </verify>
  <done>StressEngine exists with hourly + daily output, non-linear scale behavior, and passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build EnergyBankEngine as hybrid visible score + internal state</name>
  <files>app/src/main/java/com/aira/health/domain/engine/EnergyBankEngine.kt, app/src/test/java/com/aira/health/domain/engine/EnergyBankEngineTest.kt</files>
  <behavior>
    - Test 1: Recovery increases internal energy state while Strain/Stress decrease it.
    - Test 2: Visible `energyBankScore` reflects internal state and remains in 0..100.
    - Test 3: Engine remains distinct from Recovery/Strain/Stress outputs and is derived from their interaction (D-05, D-06).
  </behavior>
  <action>Implement Energy Bank per D-05 and D-06 as a dual-output model: maintain internal depletion/recharge state and expose a user-facing score each day. Recovery contributes recharge; Stress and Strain contribute depletion. Keep confidence output separate from visibility to preserve low-confidence day display (D-03, D-04, D-11, D-12).</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.EnergyBankEngineTest"</automated>
  </verify>
  <done>EnergyBankEngine exists with hybrid output semantics, distinct from source engines, and tested depletion/recharge dynamics.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                 | Description                                                                |
| -------------------------------------------------------- | -------------------------------------------------------------------------- |
| Aggregated activity/sensor streams -> non-linear engines | Untrusted physiological input crosses into high-sensitivity math functions |
| Engine outputs -> user-visible health scores             | Derived outputs impact user interpretation and decisions                   |

## STRIDE Threat Register

| Threat ID | Category | Component                         | Disposition | Mitigation Plan                                                                                                    |
| --------- | -------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| T-03-04   | T        | StrainEngine/StressEngine scaling | mitigate    | Enforce strict input bounds and output clamps; add tests for extreme values to prevent overflow or runaway output. |
| T-03-05   | I        | EnergyBankEngine internal state   | mitigate    | Maintain explicit visible/internal separation and avoid collapsing internal balance into a single opaque number.   |
| T-03-06   | R        | Confidence-vs-visibility policy   | mitigate    | Add tests that prove low-confidence inputs still produce visible scores per D-03/D-04/D-11/D-12.                   |

</threat_model>

<verification>
- .\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.StrainEngineTest"
- .\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.StressEngineTest"
- .\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.EnergyBankEngineTest"
- .\gradlew.bat :app:compileDevDebugKotlin
</verification>

<success_criteria>

- Strain and Stress implement non-linear high-end scaling per D-01 and remain bounded in 0..100.
- Energy Bank is implemented as hybrid visible/internal state per D-05 and D-06.
- Score visibility is preserved under partial data via confidence signaling (D-03, D-04, D-11, D-12).
  </success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-engines-logic/03-02-SUMMARY.md`
</output>
