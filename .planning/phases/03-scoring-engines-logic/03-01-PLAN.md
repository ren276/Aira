---
phase: 03-scoring-engines-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/aira/health/domain/engine/RecoveryEngine.kt
  - app/src/main/java/com/aira/health/domain/engine/SleepEngine.kt
  - app/src/test/java/com/aira/health/domain/engine/RecoveryEngineTest.kt
  - app/src/test/java/com/aira/health/domain/engine/SleepEngineTest.kt
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
must_haves:
  truths:
    - User gets a Recovery score every day, even when one or more inputs are missing.
    - User gets a Sleep score every day with a confidence value explaining data completeness.
    - Recovery and Sleep outputs stay bounded in 0-100 and do not crash on null inputs.
  artifacts:
    - path: app/src/main/java/com/aira/health/domain/engine/RecoveryEngine.kt
      provides: Weighted Recovery math model using HRV, RHR, Sleep, and prior Strain.
    - path: app/src/main/java/com/aira/health/domain/engine/SleepEngine.kt
      provides: Weighted Sleep math model using duration, stages, continuity, consistency.
    - path: app/src/test/java/com/aira/health/domain/engine/RecoveryEngineTest.kt
      provides: Boundary and missing-input tests for Recovery behavior.
    - path: app/src/test/java/com/aira/health/domain/engine/SleepEngineTest.kt
      provides: Boundary and missing-input tests for Sleep behavior.
  key_links:
    - from: app/src/main/java/com/aira/health/domain/engine/RecoveryEngine.kt
      to: app/src/test/java/com/aira/health/domain/engine/RecoveryEngineTest.kt
      via: Deterministic weighted formula and clamp behavior.
      pattern: 0.40/0.25/0.25/0.10 weighting with 0..100 output
    - from: app/src/main/java/com/aira/health/domain/engine/SleepEngine.kt
      to: app/src/test/java/com/aira/health/domain/engine/SleepEngineTest.kt
      via: Deterministic weighted formula and confidence downgrade behavior.
      pattern: 0.30/0.30/0.20/0.20 weighting with partial-input fallback
---

<objective>
Implement Recovery and Sleep scoring engines as pure Kotlin math components that satisfy SCORE-01 and SCORE-02 while honoring the locked confidence visibility rules.

Purpose: Establish stable, test-first score functions for daily physiology interpretation.
Output: RecoveryEngine + SleepEngine implementations and focused unit tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-engines-logic/03-CONTEXT.md
@.planning/phases/03-scoring-engines-logic/03-RESEARCH.md
@app/src/main/java/com/aira/health/data/local/model/DailyMetrics.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build RecoveryEngine with confidence-aware partial-input handling</name>
  <files>app/src/main/java/com/aira/health/domain/engine/RecoveryEngine.kt, app/src/test/java/com/aira/health/domain/engine/RecoveryEngineTest.kt</files>
  <behavior>
    - Test 1: Full-input case applies SCORE-01 weighting exactly (HRV 40%, RHR 25%, Sleep 25%, prior Strain 10%) and clamps result to 0..100.
    - Test 2: Missing one or more inputs still returns a score with reduced confidence (per D-03, D-04, D-11, D-12), never suppressing output.
    - Test 3: Extreme physiology values (very low HRV / very high RHR) still return bounded score values.
  </behavior>
  <action>Create `RecoveryEngine` as pure Kotlin with no Android imports. Implement weighted scoring per SCORE-01 using baseline-relative normalization inputs for HRV and RHR. If inputs are missing, renormalize across available weights and emit lower confidence instead of null/hidden score (per D-03, D-04, D-11, D-12). Keep all outputs deterministic and clamped to 0..100.</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.RecoveryEngineTest"</automated>
  </verify>
  <done>RecoveryEngine exists, tests pass, and low-confidence days still produce visible Recovery scores.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build SleepEngine with weighted components and confidence propagation</name>
  <files>app/src/main/java/com/aira/health/domain/engine/SleepEngine.kt, app/src/test/java/com/aira/health/domain/engine/SleepEngineTest.kt</files>
  <behavior>
    - Test 1: Full-input case applies SCORE-02 weighting exactly (Duration 30%, Stage/Deep 30%, Continuity 20%, Consistency 20%).
    - Test 2: Missing component(s) still return visible score plus downgraded confidence (per D-03, D-04, D-11, D-12).
    - Test 3: Inputs outside nominal ranges still produce bounded 0..100 output after normalization.
  </behavior>
  <action>Create `SleepEngine` as pure Kotlin with explicit component weighting for SCORE-02. Support both baseline-relative and absolute inputs where applicable, and enforce score bounds. Do not suppress score for sparse days; confidence must carry uncertainty while score remains visible (D-03, D-04, D-11, D-12).</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.SleepEngineTest"</automated>
  </verify>
  <done>SleepEngine exists, tests pass, and confidence is emitted as a parallel signal without gating score visibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest pipeline -> scoring engine | Untrusted/missing sensor values cross into deterministic math logic |
| scoring engine -> persisted daily metrics | Derived scores become durable user-visible records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | RecoveryEngine/SleepEngine input mapping | mitigate | Validate and clamp all numeric inputs before weighting; treat missing data explicitly rather than implicitly as zero. |
| T-03-02 | D | RecoveryEngine/SleepEngine | mitigate | Ensure null-safe math paths and bounded output to avoid crashes on incomplete days. |
| T-03-03 | R | Confidence-visible scoring rules | mitigate | Add deterministic tests proving score visibility with low confidence per D-03/D-04/D-11/D-12. |
</threat_model>

<verification>
- .\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.RecoveryEngineTest"
- .\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.domain.engine.SleepEngineTest"
- .\gradlew.bat :app:compileDevDebugKotlin
</verification>

<success_criteria>
- Recovery engine satisfies SCORE-01 weighting contract and returns values in 0..100.
- Sleep engine satisfies SCORE-02 weighting contract and returns values in 0..100.
- Missing-input days still produce visible scores with reduced confidence (D-03, D-04, D-11, D-12).
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-engines-logic/03-01-SUMMARY.md`
</output>
