---
phase: 04-user-interface-dashboards
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/src/main/java/com/aira/health/presentation/dashboard/home/HomeUiState.kt
  - app/src/main/java/com/aira/health/presentation/dashboard/home/HomeViewModel.kt
  - app/src/main/java/com/aira/health/presentation/dashboard/home/HomeDashboardScreen.kt
  - app/src/main/java/com/aira/health/presentation/dashboard/home/components/MetricGridCard.kt
  - app/src/main/java/com/aira/health/presentation/dashboard/home/components/CausalAnomalyCard.kt
  - app/src/main/java/com/aira/health/presentation/dashboard/home/components/ForecastGuidanceCard.kt
  - app/src/main/java/com/aira/health/presentation/dashboard/home/state/HomeDeltaAnimator.kt
  - app/src/test/java/com/aira/health/presentation/dashboard/home/HomeViewModelTest.kt
  - app/src/androidTest/java/com/aira/health/presentation/dashboard/home/HomeDashboardTest.kt
autonomous: true
requirements:
  - UI-02
  - UI-04
must_haves:
  truths:
    - Home screen renders the clinical 2x2 card order exactly: Recovery, Sleep, Strain, Stress.
    - Cached local values appear immediately, then silent foreground sync updates scores with animated deltas and confidence/last-updated context.
    - Causal Anomaly card is always visible and shows prevention/forecast guidance when no active anomaly is present.
  artifacts:
    - path: app/src/main/java/com/aira/health/presentation/dashboard/home/HomeViewModel.kt
      provides: Local-first dashboard state orchestration and silent refresh trigger.
    - path: app/src/main/java/com/aira/health/presentation/dashboard/home/HomeDashboardScreen.kt
      provides: Home tab composition with fixed 2x2 metric grid and anomaly block.
    - path: app/src/main/java/com/aira/health/presentation/dashboard/home/components/CausalAnomalyCard.kt
      provides: Always-present anomaly card with fallback forecast mode.
    - path: app/src/main/java/com/aira/health/presentation/dashboard/home/state/HomeDeltaAnimator.kt
      provides: Score delta animation policy after sync updates.
  key_links:
    - from: app/src/main/java/com/aira/health/presentation/dashboard/home/HomeViewModel.kt
      to: app/src/main/java/com/aira/health/data/worker/HealthSyncWorker.kt
      via: silent refresh trigger
      pattern: cached-first render then HealthSyncWorker.scheduleImmediate
    - from: app/src/main/java/com/aira/health/presentation/dashboard/home/HomeViewModel.kt
      to: app/src/main/java/com/aira/health/data/local/dao/DailyMetricsDao.kt
      via: Room Flow subscription
      pattern: observeByDate/observeRecent mapped to HomeUiState with confidence metadata
    - from: app/src/main/java/com/aira/health/presentation/dashboard/home/HomeDashboardScreen.kt
      to: app/src/main/java/com/aira/health/presentation/navigation/AiraNavHost.kt
      via: onMetricTap callback
      pattern: metric card click emits route id for full-screen detail navigation
---

<objective>
Build the Home dashboard vertical slice with local-first state, fixed clinical card order, always-on anomaly insight behavior, and motion/accessibility semantics from the Phase 04 contract.

Purpose: Deliver UI-02 behavior exactly while reusing the foundation from 04-01 and preserving data confidence transparency.
Output: Home ViewModel/state mapper, dashboard composables, anomaly fallback card, and focused tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-user-interface-dashboards/04-CONTEXT.md
@.planning/phases/04-user-interface-dashboards/04-UI-SPEC.md
@.planning/phases/04-user-interface-dashboards/04-RESEARCH.md
@.planning/phases/04-user-interface-dashboards/04-01-PLAN.md
@app/src/main/java/com/aira/health/data/local/dao/DailyMetricsDao.kt
@app/src/main/java/com/aira/health/data/local/model/DailyMetrics.kt
@app/src/main/java/com/aira/health/data/worker/HealthSyncWorker.kt

<interfaces>
From app/src/main/java/com/aira/health/data/local/dao/DailyMetricsDao.kt:
```kotlin
fun observeByDate(date: String): Flow<DailyMetrics?>
fun observeRecent(limit: Int = 30): Flow<List<DailyMetrics>>
```

From app/src/main/java/com/aira/health/data/local/model/DailyMetrics.kt:
```kotlin
val recoveryScore: Int
val sleepScore: Int
val strainScore: Int
val stressScore: Int
val dataConfidence: Float
val calculatedAt: Long
```

From app/src/main/java/com/aira/health/data/worker/HealthSyncWorker.kt:
```kotlin
fun scheduleImmediate(context: Context)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cached-first Home state pipeline and silent fast-sync orchestration (D-08)</name>
  <files>app/src/main/java/com/aira/health/presentation/dashboard/home/HomeUiState.kt, app/src/main/java/com/aira/health/presentation/dashboard/home/HomeViewModel.kt, app/src/main/java/com/aira/health/presentation/dashboard/home/state/HomeDeltaAnimator.kt, app/src/test/java/com/aira/health/presentation/dashboard/home/HomeViewModelTest.kt</files>
  <behavior>
    - Test 1: ViewModel emits cached Room-backed state immediately on subscription.
    - Test 2: Pull-to-refresh or foreground refresh triggers HealthSyncWorker.scheduleImmediate without blocking existing UI data.
    - Test 3: Score changes after sync produce deterministic delta animation payloads and always include confidence + last-updated metadata.
  </behavior>
  <action>Implement Home state holder per D-08 using StateFlow from DailyMetricsDao and local-first mapping rules from 03-CONTEXT confidence policy. Trigger silent foreground fast-sync through HealthSyncWorker.scheduleImmediate and keep confidence/timestamp fields mandatory in HomeUiState. Do not introduce spinner-first loading when cached values exist.</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.presentation.dashboard.home.HomeViewModelTest"</automated>
  </verify>
  <done>Home state pipeline is local-first, refresh is non-blocking, and metadata for confidence/last-updated is always present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Home dashboard layout with fixed 2x2 order and always-on anomaly card (D-07, D-09)</name>
  <files>app/src/main/java/com/aira/health/presentation/dashboard/home/HomeDashboardScreen.kt, app/src/main/java/com/aira/health/presentation/dashboard/home/components/MetricGridCard.kt, app/src/main/java/com/aira/health/presentation/dashboard/home/components/CausalAnomalyCard.kt, app/src/main/java/com/aira/health/presentation/dashboard/home/components/ForecastGuidanceCard.kt</files>
  <behavior>
    - Test 1: Grid order is exactly Recovery, Sleep, Strain, Stress and cannot be reshuffled by runtime data ordering.
    - Test 2: Causal Anomaly card is always rendered; when anomaly payload is null, forecast/preventive guidance content is shown.
    - Test 3: Metric cards display score + confidence + updated-at context and route tap callbacks for details.
  </behavior>
  <action>Implement Home dashboard composables to enforce D-07 and D-09 exactly. Reuse shared atoms from 04-01 for score rings and confidence metadata. Render anomaly/fallback card as a constant section below the 2x2 grid. Keep design contract spacing/typography from 04-UI-SPEC and avoid introducing alternate layouts.</action>
  <verify>
    <automated>.\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.presentation.dashboard.home.HomeViewModelTest" :app:compileDevDebugKotlin</automated>
  </verify>
  <done>Home screen visually and behaviorally matches locked IA/order rules and anomaly persistence requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Home dashboard tests for order, anomaly persistence, and refresh behavior (D-07, D-08, D-09)</name>
  <files>app/src/androidTest/java/com/aira/health/presentation/dashboard/home/HomeDashboardTest.kt</files>
  <behavior>
    - Test 1: UI test asserts 2x2 card semantic tags appear in the locked order.
    - Test 2: UI test asserts anomaly card remains visible in both active-anomaly and fallback-forecast states.
    - Test 3: UI test verifies refresh interaction does not clear existing cards before sync completion.
  </behavior>
  <action>Create Compose instrumentation tests anchored to semantic tags and deterministic fake state fixtures. Validate D-07/D-08/D-09 contract directly in UI tests. Keep fixtures local to androidTest to avoid leaking testing-only code into production composables.</action>
  <verify>
    <automated>.\gradlew.bat :app:connectedDevDebugAndroidTest --tests "com.aira.health.presentation.dashboard.home.HomeDashboardTest"</automated>
  </verify>
  <done>Dashboard contract is regression-protected by runnable Compose UI tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Room metrics/confidence -> Home UI mapping | Potentially partial or stale data becomes high-salience dashboard cards |
| user refresh action -> worker scheduling | Repeated user actions can flood fast-sync scheduling if not bounded |
| anomaly inference payload -> card rendering | Missing or malformed anomaly data can break card rendering semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T/I | HomeUiState mapper | mitigate | Validate and clamp mapped score/confidence ranges; always supply fallback labels and timestamps. |
| T-04-05 | D | HomeViewModel refresh trigger | mitigate | Debounce/serialize refresh intents and avoid blocking UI thread while scheduling fast-sync work. |
| T-04-06 | R | CausalAnomalyCard | mitigate | Add UI tests for both anomaly-present and anomaly-absent branches to ensure always-visible card behavior. |

</threat_model>

<verification>
- .\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.presentation.dashboard.home.HomeViewModelTest"
- .\gradlew.bat :app:testDevDebugUnitTest --tests "com.aira.health.presentation.dashboard.home.HomeViewModelTest" :app:compileDevDebugKotlin
- .\gradlew.bat :app:connectedDevDebugAndroidTest --tests "com.aira.health.presentation.dashboard.home.HomeDashboardTest"
</verification>

<success_criteria>
- UI-02 is implemented with fixed 2x2 ordering and always-present Causal Anomaly card behavior.
- D-08 cached-first rendering + silent sync + delta animation is verifiably implemented.
- Home surface displays confidence and last-updated context as first-class signals.
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-interface-dashboards/04-02-SUMMARY.md`
</output>
